/**
 * Italian eInvoice API
 * The Italian eInvoice API is a RESTful API that allows you to send and receive invoices through the Italian [Servizio di Interscambio (SDI)][1], or Interchange Service. The API is designed by Invoicetronic to be simple and easy to use, abstracting away SDI complexity while providing complete control over the invoice send/receive process. The API also provides advanced features as encryption at rest, invoice validation, multiple upload formats, webhooks, event logging, client SDKs for commonly used languages, and CLI tools.  For more information, see  [Invoicetronic website][2]  [1]: https://www.fatturapa.gov.it/it/sistemainterscambio/cose-il-sdi/ [2]: https://invoicetronic.com/
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: support@invoicetronic.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import DettaglioPagamento from './DettaglioPagamento';

/**
 * The DatiPagamento model module.
 * @module model/DatiPagamento
 * @version 1.0.0
 */
class DatiPagamento {
    /**
     * Constructs a new <code>DatiPagamento</code>.
     * @alias module:model/DatiPagamento
     */
    constructor() { 
        
        DatiPagamento.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>DatiPagamento</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/DatiPagamento} obj Optional instance to populate.
     * @return {module:model/DatiPagamento} The populated <code>DatiPagamento</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new DatiPagamento();

            if (data.hasOwnProperty('condizioni_pagamento')) {
                obj['condizioni_pagamento'] = ApiClient.convertToType(data['condizioni_pagamento'], 'String');
            }
            if (data.hasOwnProperty('dettaglio_pagamento')) {
                obj['dettaglio_pagamento'] = ApiClient.convertToType(data['dettaglio_pagamento'], [DettaglioPagamento]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>DatiPagamento</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>DatiPagamento</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['condizioni_pagamento'] && !(typeof data['condizioni_pagamento'] === 'string' || data['condizioni_pagamento'] instanceof String)) {
            throw new Error("Expected the field `condizioni_pagamento` to be a primitive type in the JSON string but got " + data['condizioni_pagamento']);
        }
        if (data['dettaglio_pagamento']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['dettaglio_pagamento'])) {
                throw new Error("Expected the field `dettaglio_pagamento` to be an array in the JSON data but got " + data['dettaglio_pagamento']);
            }
            // validate the optional field `dettaglio_pagamento` (array)
            for (const item of data['dettaglio_pagamento']) {
                DettaglioPagamento.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {String} condizioni_pagamento
 */
DatiPagamento.prototype['condizioni_pagamento'] = undefined;

/**
 * @member {Array.<module:model/DettaglioPagamento>} dettaglio_pagamento
 */
DatiPagamento.prototype['dettaglio_pagamento'] = undefined;






export default DatiPagamento;

